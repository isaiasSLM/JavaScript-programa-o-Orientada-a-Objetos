--------------------------------------------------OBSEVAÇÕES Admin --------------------------------------------------------------------
(Admin extends User) Significa que Admin vai usar partes de User.
super é de super classe. A class "admin" vai herdar o construtor() da class base que "User".

--------------------------------------------------OBSEVAÇÕES Admin --------------------------------------------------------------------

------------------------------------------------- OBSEVAÇÕES User ---------------------------------------------------------------------
 
 Para exportar é necessário avisar ao JS que vamos usar essa sintaxe fazendo umas instalações.
 terminal -> nom init y | vai criar um arquivo package.json na raiz do projeto.
 no arquivo package vai inserir o comado. "type" :"module",
 get: É um método assessor. Ela vai da permissão para visualizar essa propriedade.
 set: Vai permitir as class subordinadas alterar as propriedades. OBS set aceita apenas um parâmetro como argumento. 

------------------------------------------------- OBSEVAÇÕES User ---------------------------------------------------------------------

--------------------------------------------------OBSEVAÇÕES index --------------------------------------------------------------------
 
 ENCAPSULAMENTO: É uma forma de proteger as propriedades da class. Evitando alterações indevidas e bugs.
 Existe duas ferramentas: ATRIBUTOS PRIVADOS E MÉTODOS ASSESORES.
 Atributos privados.
 Usamos # antes do contructor() e nas propriedades.

--------------------------------------------------OBSEVAÇÕES index --------------------------------------------------------------------
